import React from 'react';

class Header extends React.Component {
  render() {
    return (
      <header>
        {' '}
        {/* pesquisar como rederizar uma imgem de fundo para a tag header. */}
        <h1>Sistema Solar</h1>
      </header>
    );
  }
}

export default Header;
